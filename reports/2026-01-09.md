# Daily Report — DRF Backend (weather-apis)
**Date:** 2026-01-09 (EAT)

## Done
- Reproduced the integration failure and captured the exact DRF error contract:
  - `HTTP 403` with `Invalid Nextcloud signature`
  - `code=missing_headers` / `reason=Missing Nextcloud HMAC headers`
- Verified the configured `CLIENT_ID` exists in `INTEGRATION_HMAC_CLIENTS_JSON` in at least one shell session, confirming config can be correct when env is loaded properly.
- Identified environment/session inconsistency as a recurring cause during debugging (e.g., `KeyError: 'CLIENT_ID'` when variables aren’t exported in the current shell).

## In progress
- Locking the HMAC request contract end-to-end:
  - Required headers (names + casing), canonicalization rules, timestamp format, nonce, body hashing rules (if any), and signature algorithm inputs.
- Adding safe, debug-only signature trace logs (server-side) behind an env toggle so we can compare “what was signed” vs “what arrived” without leaking secrets.

## Next
- Implement/verify a “golden vector” test fixture:
  - Known request + known secret → expected signature (used by unit tests)
- Add/adjust DRF unit tests to assert:
  - Missing headers → `missing_headers`
  - Bad signature → `invalid_signature`
  - Stale/future timestamp → `timestamp_skew`
  - Replay nonce → `replay_detected`
- Verify the Nextcloud “Test connection” call is actually sending the HMAC headers you expect (method + path + body match what DRF signs/validates).

## Blockers
- Header mismatch between systems (missing or differently named headers), causing DRF to reject before signature verification.
- Shell/env drift during testing (variables present in one session but missing in another), making results look inconsistent.

## Links
- Local error payload captured:
  - `{"status":1,"message":"Invalid Nextcloud signature","errors":{"detail":"Invalid Nextcloud signature","code":"missing_headers","reason":"Missing Nextcloud HMAC headers"}}`

