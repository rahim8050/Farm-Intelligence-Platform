# Daily Report — 2025-12-31 (EAT)

## Done
- Integration JWT minting was updated to set `sub` / `user_id` from the API key’s user.
- Legacy `user_id`-only tokens are still accepted.
- `whoami` claim lookup was hardened.
- Tests for token minting and backward compatibility were updated.

## In Progress
- End-to-end validation of the HMAC flow against `whoami`.
- Environment config hardening for missing or invalid variables.

## Next
- Run the full hygiene sweep:
  - `ruff check .`
  - `ruff format .`
  - `mypy .`
  - `bandit -c pyproject.toml -r .`
  - `pytest`
  - `python manage.py runserver` + smoke tests
- Fix config pitfalls:
  - ensure `WHOAMI_PATH` has a safe default
  - guard JSON parsing for Nextcloud HMAC client config

## Blockers
- Missing `WHOAMI_PATH`
- `JSONDecodeError` during HMAC client config parsing

## Links / Evidence
- `integrations/tests/test_integration_token.py`
- `KeyError: 'WHOAMI_PATH'`
- `JSONDecodeError` in `config/settings.py`
