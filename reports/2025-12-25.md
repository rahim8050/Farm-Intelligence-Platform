# Daily Report — 2025-12-25 (EAT)

## Done
- CI was unblocked by fixing the `mypy_django_plugin` failure caused by `django_extensions` being referenced in `INSTALLED_APPS` without being available in the CI dependency install.
- OpenAPI endpoints were implemented and verified:
  - `/api/schema/`
  - `/api/docs/`
  - `/api/redoc/`
- Docs/schema access was hardened at the reverse-proxy layer so those endpoints are not publicly browsable.
- Reverse-proxy header issues were traced and resolved, and the project was verified through the reverse proxy route.
- API key flow was confirmed in logs, including key creation and authenticated integration ping access.

## In Progress
- Nextcloud HMAC signing hardening and edge-case alignment.

## Next
- Finalize HMAC contract behavior and tests:
  - canonical query ordering
  - timestamp window enforcement
  - nonce replay protection
  - unknown client id behavior
  - constant-time signature comparison
  - strict `403/200` responses per failure mode
- Add reverse-proxy requirements and integration call guidance to the docs.

## Blockers / Risks
- Reverse proxy header forwarding drift can cause misleading auth/docs failures.
- Integrations are mounted at both `/api/v1/integration/` and `/api/v1/integrations/`, which can create confusion unless a single path is used consistently.

## Links
- CI run: [https://github.com/rahim8050/farm-intelligence-platform/actions/runs/20507643676](https://github.com/rahim8050/farm-intelligence-platform/actions/runs/20507643676)
- Routes:
  - `/`
  - `/admin/`
  - `/api/schema/`
  - `/api/docs/`
  - `/api/redoc/`
  - `/api/v1/auth/`
  - `/api/v1/keys/`
  - `/api/v1/integration/`
  - `/api/v1/integrations/`
