# Daily Report — 2026-01-01 (EAT)

## Done
- HMAC request signing flow exists on the DRF side.
- Integration JWT minting was updated to normalize `sub` and `user_id`.
- `whoami` claim lookup was hardened.
- Integration token tests were updated and a backward-compatibility test was added.

## In Progress
- Aligning DRF config expectations with what Nextcloud saves.
- Hardening runtime parsing of the HMAC clients config.
- Re-running smoke tests without env-var gaps.

## Next
- Make DRF settings parsing fail closed instead of crashing.
- Confirm the canonical `clientId` matches Nextcloud exactly.
- Run the full quality gate:
  - `ruff check .`
  - `ruff format .`
  - `mypy .`
  - `bandit -c pyproject.toml -r .`
  - `pytest`
- Capture an end-to-end smoke run.

## Blockers / Risks
- Nextcloud config key drift can break auth even if HMAC is correct.
- Invalid or empty HMAC client JSON can crash DRF unless guarded.
- Missing exported env vars can break smoke scripts.

## Verification Commands
- `python manage.py runserver 127.0.0.1:8001`
- `bash tools/nc_smoke.sh`
- `curl -sS "$BASE_URL/api/v1/integrations/nextcloud/ping/" -D- | head`
