# Daily Report — 2026-06-20

## Summary

Fixed radio health probe to cover all 102 stations (was only covering 37/102 due to Celery timeout). Root cause: sequential probing with redirect-following. Fixed with parallel probing + removed redirect-following (3xx/405 now treated as reachable). Added Celery task name CI check.

## Changes

### Health probe parallelisation
- `radio/services.py`: `probe_all_active_stations` now uses `ThreadPoolExecutor(10)` with 250s wait. Removed `follow_redirects=True` — 3xx/405 responses are valid health signals (server is alive). 405 no longer triggers a GET fallback that was hanging on musicradio.com servers.
- `radio/tests/test_services.py`: Updated 405 fallback test to expect 405 (not 206) as reachable.

### Radio Browser seeding (carried forward)
- `radio/management/commands/load_stations.py`: Fetch top 100 from Radio Browser API, field truncation for DB column limits, country name normalisation.
- `docs/architecture/radio/01_system_overview.md`: Removed `somafm_lounge` reference.

### Celery CI validation
- `scripts/check_celery_tasks.py`: New script — validates every `CELERY_BEAT_SCHEDULE` and routing entry resolves to a registered task.
- `.github/workflows/ci.yml`: Added `Celery task name validation` step that runs the script.

## Stats

- 102 stations: 89 available, 15 unavailable, 0 unchecked
- Full health probe completes in ~21s (was ~300s and always timed out)
- 29 registered Celery tasks, 20 beat entries, 12 route entries — all match

## Nextcloud

- Added `<hr>` separator between radio section and devallow checkbox to fix visual overlap.
- `templates/settings/admin.php`: 1 insert.
