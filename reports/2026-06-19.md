# Daily Report — 2026-06-19

## Summary

Investigated NDWI 403 on farm 29 — both NDWI and NDVI return 403 for farms without `FarmIntegrationAccess` configured. `BaseFarmView._get_farm_for_request` requires `integration_access__client_id` for integration JWT tokens, while `BaseFarmWeatherView._get_farm` only checks `is_active`. No code changes to Django — issue is Nextcloud-side integration access configuration.

## Investigation

- NDWI and NDVI share the same auth path (`BaseFarmView` → `_get_farm_for_request`). Both return 403 on farm 29 because no `FarmIntegrationAccess` record links the farm to the Nextcloud integration client.
- Weather endpoints (`BaseFarmWeatherView._get_farm`) bypass the integration-access check — that's why weather works on farm 29 while NDVI/NDWI don't.
- No NDWI observation data exists (0 rows in `ndvi_ndviobservation WHERE index_type='NDWI'`; 486 NDVI rows exist).
- No Django-side changes were needed. All fixes were in the Nextcloud app (see NextCloud-App-Customisation repo report).

## Gate

- No Django code modified.
