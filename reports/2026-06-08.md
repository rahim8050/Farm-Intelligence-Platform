# Daily Report — 2026-06-08 (Radio Phase 8+10 shipped, Activities Phases 4+5 shipped)

## Summary

Shipped the remaining three unchecked radio items (Radio Browser provider, signed stream URLs, client-driven stop events, now-playing artwork/album enrichment) and then shipped all remaining unchecked activities items (handler exception hierarchy, circuit breaker, dead letter handling, conditional recurrence, activity chaining, NDVI event listener, metrics, rate limiting). All radio and activities checkboxes are now done. Also aligned all app-level READMEs with the current response envelope shape.

## DRF repo changes

Range: `e9f73485..7720111f` (5 commits, +2 117 / −51 across 23 files).

| Commit | Description | Files | Δ |
|--------|-------------|-------|---|
| `e9f73485` | Align app-level READMEs with current envelope shape | 10 | +279 / −38 |
| `d61db606` | Phase 8 — Radio Browser provider + signed stream URLs | 8 | +431 / −5 |
| `063b8cd9` | Mark signed URLs shipped in remaining-work table | 1 | +30 / −0 |
| `07bbe585` | Phase 10 — stop events + now-playing album/artwork | 4 | +552 / −6 |
| `7720111f` | Phase 4 + 5 — activities unchecked boxes | 12 | +1022 / −29 |

### README alignment (`e9f73485`)

Updated all 8 app-level READMEs to match the current response envelope shape (`success_response` wrapper). Apps: accounts, activities, alerts, api_keys, farms, ndvi, podcasts, radio, weather.

### Phase 8 — Radio Browser provider + signed stream URLs (`d61db606`)

- **Provider** (`radio/providers/radiobrowser.py`): `RadioBrowserProvider` — fetches the top ~30k community-maintained radio stations from radio-browser.info. Open API, no key required. Inherits from `RadioProvider` base, registers via `register_provider("radiobrowser", ...)`. Caches the response per instance. Falls back silently to empty list on network errors.
- **Management command** (`radio/management/commands/load_stations.py`): Extended to support `--provider radiobrowser` flag.
- **Views** (`radio/views.py`): `SignedStreamView` — `GET /api/v1/radio/stations/<id>/stream/signed/`. Returns a time-limited HS256 JWT (default 1h TTL, configurable via `RADIO_SIGNED_STREAM_TTL_SECONDS`). Auth-gated (`IsAuthenticated`). Records a `ListeningHistory` row as a side effect.
- **URLs** (`radio/urls.py`): `radio/stations/<str:station_id>/stream/signed/` route.
- **Services** (`radio/services.py`): `record_listening_session` refactored to accept optional `request` for user-agent extraction.
- **Tests**: `test_providers.py` (17 tests covering RadioBrowser + existing providers), `test_signed_stream.py` (8 tests covering auth, token shape, expiry, side effects, station-not-found).

### Phase 10 — Stop events + now-playing album/artwork (`07bbe585`)

- **Stop endpoint** (`radio/views.py`): `ListeningSessionStopView` — `POST /api/v1/radio/history/<id>/stop/`. Auth-gated, validates ownership via `stop_listening_session`. Idempotent (already-stopped sessions return 200, not 400).
- **Service** (`radio/services.py`): `stop_listening_session(user, session_id)` — sets `stopped_at` and `duration_seconds`. Returns `(session, created)` tuple for idempotency signaling.
- **Now-playing enrichment** (`radio/services.py`): `_enrich_with_album_artwork(artist, title)` — queries the Deezer public search API (no key needed). 3s timeout, best-effort (returns `{}` on any error). Extracts `album` title and `artwork_url` (cover_medium/cover_big). Integrated into `refresh_now_playing()` so every now-playing poll also looks up album metadata.
- **Tests**: `test_stop_session.py` (8 tests — service-layer auth/ownership/idempotency + endpoint auth/shape/status codes). `test_now_playing.py` (9 tests — Deezer enrichment success, missing data, network errors, integration with ICY polling).

### Activities Phases 4 + 5 — All unchecked boxes (`7720111f`)

**Handler exception hierarchy** (`activities/handlers/base.py`):
- `HandlerError(Exception)` — base, `temporary: bool` flag
- `TemporaryHandlerError` — `temporary=True`, retryable by Celery
- `PermanentHandlerError` — `temporary=False`, no retry

**Circuit breaker** (`activities/circuit_breaker.py`):
- Cache-backed (shared across workers). Threshold: 5 failures in 300s window.
- `record_failure(handler_type)`, `record_success(handler_type)`, `is_open(handler_type)`, `can_try_half_open(handler_type)`, `get_breaker_state(handler_type)`.
- Half-open mode: allows one probe request per reset window when breaker is open.
- 3 new Prometheus counters: `activities_circuit_breaker_trips_total`, `activities_circuit_breaker_resets_total`.

**Dead letter handling** (`activities/dead_letter.py`):
- Cache-backed store (7-day TTL, no DB migration). `register_dead_letter(activity_id, ...)`, `list_dead_letters()`, `replay_dead_letter(activity_id)`, `count_dead_letters()`, `clear_all_dead_letters()`.
- Replay resets activity to `PENDING` and clears retry/execution state.
- New counter: `activities_dead_letter_count_total`.

**Conditional recurrence** (`activities/services.py`):
- `reschedule_recurring()` extended with `handler_result_metadata` param. When `{"conditional_skip": True}`, the reschedule is gated — the activity stays in its terminal state and no new row is created.

**Activity chaining** (`activities/services.py`):
- `chain_activity(source, target_type, ...)` creates a `PENDING` follow-up activity linked to the same user/farm, with `chained_from` metadata tracking provenance.
- `_chain_ndvi_actions()` in `tasks.py` — called after successful ndvi_trigger execution, creates one chained activity per `recommended_actions` entry.
- New counter: `activities_chained_count_total`.

**NDVI event listener** (`activities/handlers/ndvi_trigger.py`):
- `on_ndvi_job_completed()` — integration point for the NDVI processing pipeline. Cache-idempotent (300s TTL). Looks up existing ndvi_trigger activity for the farm, chains a new trigger. Returns `{"triggered": bool, "activity_id": int|None, "message": str}`.
- New counter: `activities_ndvi_event_count_total`.

**Metrics** (`activities/metrics.py`): 5 new counters: circuit breaker trips, circuit breaker resets, dead letter count, chained activity count, NDVI event count.

**Rate limiting** (`config/settings.py` + `activities/views.py`):
- New throttle scope `"activities": "60/min"` in `config/settings.py`.
- `throttle_scope = "activities"` added to `ActivityViewSet` and `ActivityHealthView`.

**NDVI trigger handler** (`activities/handlers/ndvi_trigger.py`):
- `NdviTriggerHandler` with `FarmState` enum (establishment, full_canopy, decline, growth, unknown), `RecommendedAction` enum (fertilizer, irrigation, vaccination), `ALLOWED_ACTIONS` frozenset allowlist, `DEFAULT_STATE_ACTIONS` mapping.
- Metadata schema validation (TypedDict), idempotency lock (Redis, 5 min TTL), state transition tracking (cache, 24h TTL), action allowlist validation.
- Reads farm state via `ndvi.farm_state.build_farm_state()`.

**Docs**: Checkboxes marked done in `01_technical_design.md` (Phase 4 + 5). Hardening review `02_hardening_review.md` updated with all implemented items. Radio `08_future_expansion.md` remaining-work table cleared. Radio `IMPLEMENTATION_SUMMARY.md` updated with Phases 8/9/10 rows.

**Tests** (`activities/tests/test_phase5.py`): 25 new tests:
- Exception hierarchy (4) — carries metadata, type check, permanent not retryable, temporary retryable
- Circuit breaker (5) — initial closed, trips at threshold, few failures ok, half-open probe, resets on success
- Dead letter (7) — register+list, idempotent register, replay existing, replay nonexistent, count, clear
- Conditional recurrence (3) — normal reschedule, conditional skip, interval reschedule
- Activity chaining (2) — chain creates pending, returns none without owner
- NDVI event listener (2) — triggers chain on event, no-existing-activity returns not triggered
- NDVI trigger handler (3) — handler registered, returns HandlerResult, default state actions

## Repository state

| Repo | Branch | HEAD | Clean? |
|------|--------|------|--------|
| DRF (`farm-intelligence-platform`) | `main` | `7720111f` | ✅ yes |
| Nextcloud (`farm_intelligence_platform`) | `master` | `53708ab5` | ✅ yes (no changes today) |
