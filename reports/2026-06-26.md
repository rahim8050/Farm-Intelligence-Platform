# Daily Report — June 26, 2026

## Overview

Phase 1 spectral engine merged (SpectralComputeEngine, StacDataProvider, band registry), automation architecture documented, stack trace exposure fixed in activities serializer, and test coverage hardened for env-independent CI.

## Commits

### `f06977f8` — phase 1 spectral engine

| File | Change |
|------|--------|
| `ndvi/engines/compute.py` | **New** — `SpectralComputeEngine` class: generic index computation via `DataProvider` + `IndexDefinition`, no `if index_type` branches |
| `ndvi/providers/base.py` | **New** — `DataProvider` Protocol with `search()`, `get_latest()`, `load_band()` interface |
| `ndvi/providers/stac.py` | **New** — `StacDataProvider` implementation wrapping `StacClient` + per-band COG loading |
| `ndvi/engines/gee.py` | Refactored to use `_load_gee_index_array` (generic) replacing `_load_gee_ndvi`/`_load_gee_ndwi`; added `_load_single_gee_band` for per-band COG download |
| `ndvi/engines/stac.py` | Same pattern: `_load_stac_index_array` replaces `_load_stac_ndvi`/`_load_stac_ndwi`/`_load_stac_ndmi` |
| `ndvi/engines/landsat.py` | Refactored to use `FORMULA_REGISTRY` + `BAND_REGISTRY` instead of hardcoded band names |
| `ndvi/engines/modis.py` | Same refactor — MODIS now uses formula-driven index computation |
| `ndvi/tests/test_phase1_compute_engine.py` | **New** — 596-line test suite for `SpectralComputeEngine`, providers, band loading, edge cases |

### `fbbe6ba6` — fix hook blockers

- Pre-commit hook compliance fixes for the Phase 1 merge

### `21142728` — fix stac dispatch table

- Fixed STAC index loader dispatch after Phase 1 refactor
- Removed stale `load_ndvi_array` / `load_ndwi_array` / `load_ndmi_array` calls from engine dispatch

### `33bb7911` — Fix missing patch import in test_ndvi_stac_engine.py

- Added missing `MagicMock` import in `test_ndvi_stac_engine.py` for `@patch` based tests

### `88da7a0a` — fix(ndmi): hardcode max_cloud in raster PNG tests to be env-independent

- Replaced `settings.NDVI_STAC_MAX_CLOUD_DEFAULT` with explicit `max_cloud=20` in all NDMI raster PNG tests so they don't depend on environment configuration

### `108e8215` — docs: Add automation and workflow architecture documentation

| File | Change |
|------|--------|
| `docs/architecture/automation_architecture.md` | **New** — 369-line document covering CI/CD pipelines, pre-commit hooks, agent runner automation, cron-based phase advancement, email notifications, and recovery procedures |

### `15fe6eab` — fix: Prevent stack trace exposure in activities serializer exception

- Wrapped raw exception in `ValidationError` with sanitised message (`"An error occurred"`) in `activities/serializers.py`
- Prevents Django debug tracebacks from leaking to API consumers

## CI Status

- Pre-commit hooks: clean (ruff, mypy)
- 596 new Phase 1 engine tests added
- Env-independent test configuration (no more `NDVI_STAC_MAX_CLOUD_DEFAULT` dependency)
