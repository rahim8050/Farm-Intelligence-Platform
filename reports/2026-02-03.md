# Daily Progress — 2026-02-03 (EAT)

## Done
- Confirmed monitoring stack is up: Prometheus, Grafana, Loki, Promtail, exporters (docker compose ps all healthy).
- Identified Nextcloud logfile + verified content:
  - datadirectory: /var/www/html/nextcloud/data
  - logfile: /var/www/html/nextcloud/data/nextcloud.log (86MB)
  - Verified via: `sudo -u www-data php -f occ log:tail 30 --raw`
- Wired Nextcloud logs into Loki via Promtail:
  - Mounted host log into promtail container (read-only): `/var/www/html/nextcloud/data/nextcloud.log -> /var/log/nextcloud/nextcloud.log:ro`
  - Added promtail scrape job `job_name: nextcloud`
  - Added JSON parsing + labels extraction (app, level)
  - Promtail confirms it’s tailing the file and target exists:
    - `Adding target ... {job="nextcloud"}`
    - `tail routine: started`
    - `Seeked ... Offset:0`

## In progress
- Validating Loki ingestion + confirming logs are queryable from Loki/Grafana.
- Investigating Loki ingestion throttling/rate limiting (429 noted earlier during initial ingest/backlog).

## Next
- Query Loki using range endpoint (log selectors require range query):
  - Use `/loki/api/v1/query_range` for `{job="nextcloud"}` and `{job="nextcloud", app="dav"}`
- If 429 persists:
  - Prefer `read_from_head: false` for the Nextcloud promtail job (avoid ingesting 86MB backlog), OR
  - Truncate/rotate Nextcloud log to reset backlog, OR
  - Tune Loki `limits_config` (ingestion rate/burst) if we want full backfill.
- Update Grafana panels to use `{job="nextcloud"}` label and verify data appears.

## Blockers
- Loki instant query endpoint rejects log selectors (“log queries are not supported as an instant query type”) → must use `query_range`.
- Potential Loki ingestion rate limiting (429) during first ingest of large Nextcloud log file.

## Links / Evidence
- Nextcloud log file: `/var/www/html/nextcloud/data/nextcloud.log`
- Promtail mounted path: `/var/log/nextcloud/nextcloud.log`
- Key commands:
  - `sudo -u www-data php -f occ status`
  - `sudo -u www-data php -f occ log:tail 30 --raw`
  - `docker compose -f docker-compose.monitoring.yml ps`
  - `docker compose -f docker-compose.monitoring.yml logs --tail=200 promtail | grep -i nextcloud`
  - Loki readiness: `curl http://127.0.0.1:3100/ready` (200)

