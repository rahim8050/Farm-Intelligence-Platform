# Daily Report — 2026-05-25 (EAT)

## Done

- **Phase 4 — Fusion and Intelligence**:
  - Added Sentinel-1 context module (`ndvi/sentinel1_context.py`) with wet soil, flooding, rough surface, urban interference flags.
  - Enhanced `FusionResult` with `quality_flags`: `source_disagreement`, `fallback_used`, `anomaly_detected`, `s1_*` flags.
  - Added 32 tests for S1 context derivation, anomaly detection, and quality flag propagation.

- **Phase 5 — API Evolution**:
  - Added `?representation=v1|v2` query param to timeseries/latest/farm-state endpoints.
  - V2 payload includes inline V2 fields (`smoothed_ndvi`, `confidence`, `source`, `quality_flags`), `v2_observations` list, and `representation` key.
  - Default (V1) payload unchanged — backward compatible.
  - Added 12 tests covering V1 vs V2 response shapes.

- **Phase 6 — Operational Hardening**:
  - Added 6 Prometheus metrics: null output rate, low confidence rate, fallback frequency, source disagreement.
  - Wired metrics into `v2_quality.py` and `fusion.py` decision tree.
  - Added GEE engine stub (`ndvi/engines/gee.py`), registered in `SUPPORTED_ENGINES`.

- **CI Fixes**:
  - Fixed mypy type errors in test files (`BBox` `Decimal` args, `date` → `isoformat()`).
  - Applied ruff format to `test_phase5_api.py` (dict literal line wrapping).

- **Analysis — API Key Farm Visibility Regression**:
  - Traced root cause of farms disappearing after API key rotation in the Nextcloud integration path.
  - Key finding: direct API key auth always works (farms linked to User, not key). Regression occurs only via the Nextcloud two-step flow (API key + HMAC → JWT → farms by `client_id`).
  - Documented in `prompts/api-key-farm-visibility-regression.md`.

## In Progress

- None.

## Next

- Deploy Phase 4–6 to staging.
- Monitor Prometheus metrics after deployment.
- Resolve API key rotation procedure for Nextcloud integration.

## Blockers / Risks

- None.

## Stats

- **Commits today:** 4
- **Lines added:** 1,389
- **Lines removed:** 27
- **Net change:** +1,362

## Links

- `ndvi/sentinel1_context.py`
- `ndvi/fusion.py`
- `ndvi/views.py`
- `ndvi/metrics.py`
- `ndvi/v2_quality.py`
- `ndvi/engines/gee.py`
- `ndvi/tests/test_phase4_fusion.py`
- `ndvi/tests/test_phase5_api.py`
- `ndvi/tests/test_gee_engine.py`
- `prompts/api-key-farm-visibility-regression.md`
