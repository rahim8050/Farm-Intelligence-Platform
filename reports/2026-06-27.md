# Daily Report — June 27, 2026

## Overview

Phase 1 spectral engine finalized: generalized GEE/STAC index loaders + services factory refactor merged to main. Fixed circuit breaker test that broke during Phase 1, added 14 coverage tests to bring new engine code to 100%/99%. Cleaned up stale tracked artifacts (Caddyfile, .codex, .dmypy.json, etc.) and migrated daily reports to proper directory.

## Commits

### `390f4ce2` — refactor(ndmi): generalize spectral index computation via FORMULA_REGISTRY

| File | Change |
|------|--------|
| `ndvi/engines/compute.py` | Added optional `engine_name` parameter to `SpectralComputeEngine.__init__()` |
| `ndvi/engines/gee.py` | Replaced `_load_gee_ndvi`/`_load_gee_ndwi` with generic `_load_gee_index_array` using `FORMULA_REGISTRY` + band-to-asset mapping + per-band COG loading via `_load_single_gee_band()` |
| `ndvi/engines/stac.py` | Same pattern: `_load_stac_index_array` replaces three hardcoded loaders (NDVI/NDWI/NDMI); `_load_single_stac_band()` for per-band COG download |
| `ndvi/services.py` | Major refactor — engine factories now use `SpectralComputeEngine` + `FORMULA_REGISTRY` + `StacDataProvider` instead of old `StacEngine`/`GeeEngine`/`LandsatEngine`/`ModisEngine` directly. Added `_with_provider_and_formula()` helper, `_gee_stac_client()`, `_landsat_stac_client()`. NDMI variants for GEE, STAC, Landsat, MODIS |
| `ndvi/tests/test_gee_engine.py` | Updated patches to mock `_load_single_gee_band` instead of old `load_ndvi_array` |
| `ndvi/tests/test_ndmi_engines.py` | Updated test assertions for generic loader API |
| `ndvi/tests/test_ndvi_stac_engine.py` | Updated patches; unknown index now returns `None` instead of `ValueError` |

### `8db1e4de` — fix: repair circuit breaker test and add coverage for spectral engines

- Fixed `test_run_ndvi_job_stac_circuit_breaker_persists_across_retries` — was failing with `AttributeError: 'SpectralComputeEngine' object has no attribute 'client'` after Phase 1 refactor. Now accesses `shared_engine.provider.client`
- Added 14 coverage tests covering:
  - `_load_gee_index_array`: unknown index, unmapped band, empty-after-SCL-mask
  - `_load_single_gee_band`: local file, HTTP download, missing CRS
  - `_load_stac_index_array`: unknown index, missing band href, empty band, SCL mask applied, empty after masking
  - `_load_single_stac_band`: local file, HTTP download, missing CRS
- Coverage: `gee.py` 100%, `stac.py` 99%

### `7b2bf205` — fix: add type casts to resolve mypy attr-defined errors in circuit breaker test

- Added `typing.cast` for `SpectralComputeEngine` and `StacDataProvider` to satisfy mypy's `attr-defined` check on `.provider.client` access

### `3d2a1c21` — chore: move tracked daily reports from prompts/ to reports/

- `git mv prompts/2026-06-24-daily-report.md reports/2026-06-24.md`
- `git mv prompts/2026-06-25-daily-report.md reports/2026-06-25.md`
- Renamed to match existing `YYYY-MM-DD.md` convention

## Housekeeping

- **Removed from tracking (git rm):** `Caddyfile`, `caddy.json`, `ndvi.png`, `ndvi_debug.png`, `ndvi/fusion_ndwi.py,cover`, `.dmypy.json`, `.opencode/run-agent.sh`, and stale root files (`import`, `PY`, `raw`, `try:`, `except`)
- **Added to `.gitignore`:** `gunicorn.ctl`
- **Deleted empty directories:** `.agents/`, `.codex/`

## CI Status

- Ruff: clean (320 files formatted)
- Mypy: clean (7 source files)
- Tests: 87 passed (43 engine + 44 task extra tests)
- Coverage on new engine code: `gee.py` 100%, `stac.py` 99%
- Pre-push hooks: all checks pass
