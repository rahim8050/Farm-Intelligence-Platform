# Daily Report — 2025-12-26 (EAT)

## Done
- Integration endpoint security was confirmed as default-deny.
- API key auth was verified end to end.
- Reverse-proxy hardening headers were verified on DRF responses.
- Server-to-server integration plumbing is stable enough to continue Nextcloud caller work.

## In Progress
- Nextcloud HMAC signing hardening and contract alignment.

## Next
- Finalize HMAC contract behavior and tests:
  - canonical query ordering
  - strict timestamp window enforcement
  - nonce replay protection
  - unknown `client_id` handling
  - constant-time signature comparison
  - predictable `403/200` responses per failure mode
  - rotation safety with current + previous secrets
- Document the Nextcloud → DRF integration contract and reverse-proxy requirements.

## Blockers / Risks
- Proxy header forwarding drift can break auth/docs behavior or host validation.
- Endpoint consistency must be maintained between Nextcloud and tests.

## Links
- `/api/v1/integration/ping/`
- `/api/v1/keys/`
- `/api/v1/integration/`
