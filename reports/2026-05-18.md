# Daily Report — 2026-05-18 (EAT)

## Done
- **Phase 3 Core Implementation** (Phases 1–9):
  - Added 10+ lifecycle fields to `NdviObservation` (version, state, is_latest, computed_at, acquired_at, ingested_at, source_scene_id, provenance, provenance_hash).
  - Implemented append-only write semantics with `select_for_update` locking.
  - Added version enforcement (`NDVI_VERSION` setting, version attachment to all writes).
  - Introduced lifecycle states (RAW, FINAL, SUPERSEDED, INVALIDATED, REJECTED) with explicit transition matrix.
  - Built batch recompute engine with bounded windows, chunking, and DB-level backpressure.
  - Hardened cloud handling: `cloud_fraction=None` cannot be FINAL.
  - Extended metrics: `ndvi_observation_latest_age_seconds`, `ndvi_version_mismatch_total`, `ndvi_recompute_trigger_total`.
  - Updated API serializers to include version, state, is_latest, computed_at.
  - Added 8 migrations (non-destructive, preserving all historical data).

- **Followup Round 1 — Distributed Systems Guarantees**:
  - Added Redis-backed circuit breaker (closed → open → half_open states).
  - Implemented queue isolation validation (`ndvi_ingestion`, `ndvi_recompute`, `ndvi_analysis`).
  - Added jitter-based retry backoff using `secrets.randbelow` (not `random.uniform`).
  - Replaced Celery inspect with DB-level backpressure query.

- **Followup Round 2 — Operational Hardening**:
  - Added provenance schema validation (`VALID_PROVENANCE_KEYS` whitelist).
  - Implemented strict canonical JSON for provenance hashing (`sort_keys`, `separators=(",",":")`, `ensure_ascii=True`).
  - Added `INVALIDATED` and `REJECTED` states with explicit transition rules.
  - Updated `NdviFarmStateService` to exclude invalid states.

- **Followup Round 3 — Semantic Versioning & Queue Isolation**:
  - Added `parse_version()` and `version_gte()` for safe semantic comparison.
  - Added `NDVI_QUEUE_ISOLATION_MODE` setting (single, subset, all).
  - Implemented fail-closed queue validation in production, fail-open in dev.
  - Made `compute_provenance_hash()` reject unsupported types instead of `default=str`.

- **Followup Round 4 — Circuit Breaker & Queue Coverage**:
  - Added 419 lines of tests covering circuit breaker, queue isolation, version-aware validity, and recompute.
  - Fixed CI test failures from queue isolation changes and partial unique constraints.
  - Added `get_recompute_queue_breakdown()` for queued/running/stuck job breakdown.

## In Progress
- Phase 3 followup Round 5 (formal version spec, retry classification, Phase 4 design).

## Next
- Complete Phase 3 Round 5 hardening.
- Fix CI coverage threshold (96%).
- Begin Phase 4 chaining implementation.

## Blockers / Risks
- CI coverage threshold at 95.33% (needs 96%).
- Pre-commit hooks must pass before commit.

## Links
- `ndvi/models.py`
- `ndvi/services.py`
- `ndvi/metrics.py`
- `ndvi/tests/test_ndvi_services.py`
- `prompts/phase3followup.md`
- `prompts/ndvi_phase3_followup_round{1,2,3,4}_report.md`
