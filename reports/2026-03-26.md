# Daily Report — 2026-03-26 (EAT)

## Done
- Investigated the Nextcloud admin UI schema parse error.
- Verified behavior after moving Nextcloud cache from APCu to Redis.

## Findings
- The schema URL injected into the admin page was correct.
- Redis was healthy and reachable.
- The route existed in the Nextcloud router.
- The UI failure was caused by stale cache or route artifacts, not the route itself.

## Resolution
- Cleared Redis cache keys.
- Farms UI schema loading was unblocked.
- Redis remained the cache backend to preserve worker token caching.

## Notes / Follow-ups
- If this reappears, confirm the route with `occ router:match` and clear Redis keys.
- Avoid APCu-only local cache for Nextcloud workers unless `apc.enable_cli=1` is set.
