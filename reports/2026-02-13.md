# Daily Report — 2026-02-13 (EAT)

## Done
- Stabilized the monitoring stack behind Caddy and validated Prometheus scraping.
- Added Grafana dashboards for system metrics and hungry endpoints.
- Investigated Nextcloud ↔ DRF integration issues around NDVI 404s and missing farm IDs.

## Findings
- The `/api/v1/farms/21/ndvi/raster.png` 404 is expected if farm 21 does not exist for the current API key.
- Curl verification showed farm 21 is not present in the DRF farm list.
- Raster jobs initially failed due to Sentinel Hub auth, then returned `image/png` after credentials were fixed.
- The root cause of the 404s is a farm ID mismatch between Nextcloud UI data and DRF data, not HMAC config.

## Notes
- Secrets should not be hashed for HMAC usage; plaintext must be available in memory.
- A local commit was blocked by file ownership and then completed as `www-data`.

## Open Questions
- Why does Nextcloud’s farm list differ from DRF’s farm list?

## Next
- Capture the Nextcloud admin/farms/list JSON.
- Curl DRF `/api/v1/farms/` with the same API key.
- Verify baseUrl consistency.
- Clear UI cache and reload if farm 21 appears only in Nextcloud.
