Report: Monitoring + Nextcloud/DRF 404 Investigation (as of Feb 13, 2026)

  Scope of work

  - Stabilized the monitoring stack (Prometheus + Grafana) behind Caddy and validated Prometheus scraping.
  - Added Grafana dashboards/panels for “hungry endpoints” and system metrics.
  - Investigated Nextcloud ↔ DRF integration issues causing NDVI 404s and missing farm IDs.

  ———

  Monitoring stack changes (completed and committed)

  - Prometheus subpath support: configured Prometheus to serve under /prometheus so it works behind Caddy.
  - Grafana datasource fix: set Grafana Prometheus URL to http://prometheus:9090/prometheus.
  - Prometheus self-scrape: added a prometheus job with metrics_path: /prometheus/metrics.
  - Redis exporter: added a Redis container and re-pointed redis_exporter to it.
  - Dashboards:
      - Updated weather-apis-system panels to use up{job="prometheus"} and scrape_duration_seconds{job="prometheus"}.
      - Added “hungry endpoints” dashboard + panels (top latency, request rate, node CPU/memory).
  - Verified Prometheus targets and Grafana panel data via queries.

  ———

  NDVI 404 investigation (current findings)

  - 404 on /api/v1/farms/21/ndvi/raster.png is expected if farm 21 does not exist for the current API key.
  - Curl verification with the same API key returned:
      - /api/v1/farms/21/ → 404
      - /api/v1/farms/ list shows IDs: 17,16,15,8,7,4,3,2 (no 21)
  - Raster jobs:
      - Queueing a raster worked.
      - Initial job failed due to Sentinel Hub auth 403.
      - After fixing credentials/engine, raster returned 200 with image/png.
  - Conclusion so far: farm ID mismatch between Nextcloud UI and DRF data is the root cause of the 404s (not HMAC config).

  Nextcloud integration facts confirmed


  ———
  - Added guidance to not hash secrets and to encrypt at rest and decrypt only in memory, since HMAC requires plaintext secret bytes.
  - You ran the change locally; commit was blocked by file ownership. You updated ownership + committed as www-data (pending my confirmation).

  ———

  Open questions for tomorrow’s testing

  1. Nextcloud list vs DRF list mismatch:
      - Compare:
          - POST /apps/weather_apis/api/v1/admin/farms/list

  ———

  Recommended test steps for tomorrow

  1. In Nextcloud UI DevTools, capture JSON for admin/farms/list.
  2. Curl DRF /api/v1/farms/ with the same API key.
  3. Verify baseUrl is identical in Nextcloud settings and DRF environment.
  4. If farm 21 appears only in Nextcloud, clear UI cache and reload farms list.
