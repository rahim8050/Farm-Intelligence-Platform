# Daily Report — 2026-05-15 (EAT)

## Done
- Fixed CI test failures: HMAC verification only computed base64 signatures but tests/clients generate hex signatures. Added dual-format support (hex + base64) with case-insensitive comparison.
- Added `farms/activity_views.py` with full CRUD endpoints for farm activities (list, create, retrieve, update, delete) with integration token scope enforcement.
- Fixed `_resolve_owner` bug where integration tokens failed because `user_id` claim is a client_id string, not a User pk. Now uses `farm.owner` for integration tokens.
- Added 39 tests covering activity views: CRUD operations, integration token access, scope enforcement, filtering, pagination, and error cases.
- Fixed mypy `union-attr` error in activity views test (`Activity.objects.first()` can return `None`).
- `farms.activity_views` coverage increased from 0% to 100%.

## In Progress
- None

## Next
- Monitor CI coverage threshold (96%) after activity views integration.
- Review remaining low-coverage modules (`activities/tasks.py`, `ndvi/farm_state.py`, `ndvi/management/commands/consume_ndvi_stream.py`).

## Blockers / Risks
- Pre-commit hooks (ruff, ruff-format, bandit, mypy) must pass before commit.

## Links
- `integrations/hmac.py`
- `farms/activity_views.py`
- `farms/tests/test_farm_activity_views.py`
- `prompts/ci_error.md`
