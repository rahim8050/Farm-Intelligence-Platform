# Daily Report - 2026-04-20

## Summary
Resolved CI failures and enhanced NDVI stream consumer reliability.

## Changes
- **CI/Tests:**
  - Updated `ndvi/tests/test_ndvi_streams.py` to match Redis stream (`ndvi:stream`) and DLQ (`ndvi:dlq`) names from `config/settings.py`.
- **Consumer Reliability (`ndvi/management/commands/consume_ndvi_stream.py`):**
  - Integrated `XAUTOCLAIM` for efficient stale message recovery.
  - Implemented configurable startup ID (`NDVI_STREAM_START_ID`) and periodic reclamation intervals (`NDVI_STREAM_RECLAIM_INTERVAL_SECONDS`).
  - Added `threading.Lock` to support graceful shutdowns that wait for in-flight tasks.
  - Enhanced observability with structured logging (tracking `message_id`, `delivery_count`, `job_type`).
  - Improved DLQ logic to use accurate Redis metadata for delivery counts.
- **Configuration (`config/settings.py`):**
  - Added new configuration variables: `NDVI_STREAM_START_ID`, `NDVI_STREAM_RECLAIM_INTERVAL_SECONDS`.
  - Updated `NDVI_STREAM_DLQ_MAXLEN` to `10000`.

## Outcome
- Resolved existing CI test failures.
- Increased consumer stability and improved diagnostic capabilities via structured logging.
