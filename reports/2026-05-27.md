# Daily Report - 2026-05-27

## Summary of Work

GitHub Actions CI minutes exhausted. Set up a local Docker-based CI pipeline that mirrors the GitHub Actions workflow and auto-runs on `git push`.

## Changes Made

### Local CI Pipeline
- **`Dockerfile.ci`** — Python 3.12-slim image with system deps (mysqlclient build) + all Python requirements pre-installed. Source is mounted live at runtime (no rebuild needed per change).
- **`scripts/run-ci.sh`** — Orchestrator: spins up MySQL 8.0 container, runs all CI checks (ruff, mypy, bandit, Django checks, migrations, pytest with 96% coverage), then cleans up.
- **`.githooks/pre-push`** — Git hook that triggers `scripts/run-ci.sh` before every push. Errors block the push.
- **`.dockerignore`** — Excludes `.venv`, caches, and build artifacts from Docker context (reduced from GB to 14MB).

### CI Checks (matches `.github/workflows/ci.yml`)
1. `ruff check .`
2. `ruff format --check .`
3. `mypy --config-file=pyproject.toml .` (252 source files)
4. `bandit -c pyproject.toml -r .` (40k+ lines)
5. Django system checks + `--deploy`
6. Migration consistency (`makemigrations --check --dry-run`)
7. `manage.py migrate` (fresh DB)
8. `pytest -x --durations=5 --cov=. --cov-fail-under=96` (1219 tests)

### Statistics
- **Files Modified:** 4 new files
- **Lines Added:** 166
- **Lines Removed:** 0

## Verification
- All static checks pass: ruff, mypy, bandit, Django checks, migrations
- Tests (1219) pass but take 15-30 min — added `-x` and `--durations=5` for faster feedback

## Next Steps
- Consider adding `pytest-xdist` for parallel test execution if test suite keeps growing

## References
- `.github/workflows/ci.yml` — mirrored workflow
- `scripts/run-ci.sh` — local CI entry point
