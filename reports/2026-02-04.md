# Daily Report — Nextcloud + DRF (2026-02-04)

## ✅ Done
- Fixed Caddyfile syntax issues (tls brace/path parsing) and validated config (`caddy fmt`, `caddy validate`, `caddy reload`).
- Confirmed Nextcloud behind Caddy on **/nextcloud**:
  - `/nextcloud/` → `302` → `/nextcloud/index.php/login`
  - `/nextcloud/index.php/login` → `200 OK`
- Fixed Grafana reverse-proxy subpath behavior:
  - Moved from `handle_path /grafana/*` to **`handle /grafana*`** (preserve prefix)
  - Grafana UI now loads at **/grafana/** without redirect loops.
- Verified tailnet HTTPS endpoint responds correctly over 443 via curl.

## 🟡 In Progress
- Hardening “subpath behind reverse proxy” settings consistency:
  - Ensure Grafana `GF_SERVER_ROOT_URL` + `GF_SERVER_SERVE_FROM_SUB_PATH=true` are set in compose (so it stays stable across restarts).
  - Confirm Nextcloud overwrite settings/trusted proxy are correct (only if any redirects reappear).

## 🔜 Next
- Capture clean screenshots for Sunday article:
  - Grafana home + key dashboard panels
  - API health/uptime panels + latency/error panels
- Add a “System Overview” dashboard row (uptime, p95 latency, 5xx rate, DB exporter, disk/mem).
- Quick doc update: Caddyfile snippets + subpath rules (Grafana/Nextcloud) + verification curl commands.

## 🚧 Blockers
- None right now.
- (Previously: browser cached redirect loop; resolved by config + clearing site data/incognito.)

## 🔗 Links
- Nextcloud: internal lab endpoint
- Grafana: internal lab endpoint
- DRF API: internal lab endpoint
- Health check: internal lab endpoint

