# Daily Report — 2026-01-31 (EAT)

## Done ✅
### STAC / NDVI Raster work
- Implemented env-backed STAC defaults (Copernicus-first):
  - Wired `NDVI_STAC_*` env settings into `config/settings.py`.
  - Updated STAC documentation in `ndvi/README.md`.
- Implemented asset fallback resolution so `raster_png` can use Copernicus assets:
  - Added asset candidate helpers in `ndvi/stac_client.py`.
  - Wired into `ndvi/engines/stac.py` and `ndvi/raster/stac_compute_engine.py`.
- Improved “Raster not found” diagnostics:
  - Expanded warning logs to include collection(s), available assets, expected asset candidates, bbox/time window/cloud limit, and trace context.
  - Ensured `RasterRequest` carries `farm_id` via `ndvi/raster/service.py`.
- Docs/notes updated:
  - `.env` is treated as dotenv (explicitly discourage `source .env`) in `README.md` and `docs/hmac_clean_setup.md`.
  - Added `FIX_STAC_RASTER_PNG.md` fix note.
- Tests added/updated:
  - `ndvi/tests/test_ndvi_raster_engines.py` (fallback + log-context coverage)
  - `tests/test_settings_ndvi_stac.py` (env-read coverage)

### Verification run (today)
- `pytest ndvi/tests/test_ndvi_raster_engines.py tests/test_settings_ndvi_stac.py`
  - Result: **16 collected → 15 passed, 1 failed**
- `ruff check .` → **All checks passed**
- `mypy .` → **Success (no issues in 134 source files)**
- `bandit -c pyproject.toml -r .` → **No issues identified**

## Findings 🔎
### Test failure (not a functional regression)
- Failing test: `test_stac_compute_engine_missing_assets_logs_context`
- Cause: test asserts a substring that depends on *ordering* of `expected_assets` in the log.
- Current log output prefers resolution variants first (e.g., `B04_10m, B04_20m, B04_60m, B04`), so the old order-based string expectation is brittle.

### TLS / Network / Caddy battle
- Tailnet HTTPS cert minting currently blocked (“not permitted to mint certificate”), so Caddy cannot use Tailscale-managed certs yet.
- Phone error `ERR_CONNECTION_REFUSED` points to listener/firewall/binding mismatch (service not reachable on :443/:80), not an application bug.

## In progress 🟡
- Make the failing log-context test robust:
  - Replace order-dependent substring assertion with semantic checks (contains required candidates and/or parse `expected_assets={...}` from log and assert structure/priority).
- Caddy cutover for TLS termination on tailnet:
  - Decide TLS path given Tailscale cert issuance restriction (Tailscale certs vs `tls internal` vs temporary HTTP).

## Next ⏭️
### Code / Tests
- Patch `ndvi/tests/test_ndvi_raster_engines.py` to avoid order-dependent log assertions.
- Re-run:
  - `pytest ndvi/tests/test_ndvi_raster_engines.py::test_stac_compute_engine_missing_assets_logs_context`
  - then `pytest ndvi/tests/test_ndvi_raster_engines.py tests/test_settings_ndvi_stac.py`
- After green: quick manual smoke test for `raster_png` on a known farm/AOI.

### TLS / Networking
- Confirm Caddy is actually listening and healthy:
  - `sudo ss -lntp | rg ':(80|443)\b'`
  - `sudo systemctl status caddy --no-pager`
  - `sudo journalctl -u caddy -n 200 --no-pager`
- Choose TLS approach:
  - If tailnet permits later: mint Tailscale cert and configure `tls <crt> <key>` in Caddy.
  - Otherwise: use `tls internal` and install Caddy root CA on phone, or temporarily use HTTP over tailnet.

## Blockers 🚧
- Tailnet policy/permission preventing Tailscale certificate minting.
- One brittle pytest assertion (log ordering) still failing until updated.

## Commands run 📌
- `pytest ndvi/tests/test_ndvi_raster_engines.py tests/test_settings_ndvi_stac.py`
- `ruff check .`
- `mypy .`
- `bandit -c pyproject.toml -r .`

