## DRF Daily Report — 2026-01-16 (EAT)

### Done
- Debugged NDVI raster endpoint behavior: confirmed responses were JSON despite `.png` path; reproduced `406 Not Acceptable` when sending `Accept: image/png` and captured headers/body (`Vary: Accept`, JSON envelope).
- Verified repo hygiene: confirmed `logs/`, `db.sqlite3`, and `__pycache__/` are ignored by `.gitignore`.
- Audited tracked files: confirmed `api_keys/` is tracked as a Django app (code), and found `celerybeat-schedule` is mistakenly tracked (runtime artifact).

### In progress
- Patch NDVI raster view to always return `image/png` on success (ignore `Accept`) and keep JSON envelope for errors.
- Update OpenAPI schema/docs so 200 is `image/png` (Swagger currently shows `application/json`).

### Next
- Untrack and ignore `celerybeat-schedule`, commit repo hygiene change.
- Implement view change (use `HttpResponse`/`FileResponse` for PNG) + update schema annotations.
- Run only affected pytest tests for the NDVI raster endpoint; then run ruff/mypy/bandit.

### Blockers
- None (Pillow not installed, but not required; can validate PNG via headers + magic bytes test).

### Links
- NDVI raster endpoint: `/api/v1/farms/17/ndvi/raster.png?date=2026-01-16&max_cloud=30&size=148`
- Captured error: `HTTP 406` with JSON message “Could not satisfy the request Accept header.”

