# Legacy compatibility stub.
#
# The dependency source of truth for this repository is `pyproject.toml`.
# Use `uv sync --no-install-project --group dev` to install the local
# development environment.
